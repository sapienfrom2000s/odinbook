<%= form_with(model: @friendship, scope: :friendship, method: :delete) do |form| %>
    <%= form.hidden_field :friendrequest_id, value: friendrequest_id %>
    <%= form.submit "#{name}", data: { turbo_confirm: "Are you sure?" } %>
<% end %>